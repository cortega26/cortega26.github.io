---
interface Props {
  lang: 'en' | 'es';
}
const { lang } = Astro.props;

const en = {
  availability: 'Available for scoped automation projects',
  meta: 'Tooltician · Carlos Ortega Gonzalez',
  title: 'Python automation for workflows that need to keep working after handoff.',
  summary: 'I build reporting, ETL, and internal tools that replace fragile manual work with systems a team can operate, audit, and trust.',
  support: 'Based in Santiago, Chile. I work in English and Spanish with teams across LATAM and U.S.-friendly time zones.',
  highlights: [
    'Reporting automation, ETL pipelines, and data collection workflows built for repeatable operation.',
    'Internal APIs and tooling delivered with CI, tests, and handoff documentation.',
    'Direct communication with technical and non-technical stakeholders in English or Spanish.',
  ],
  ctaPrimary: 'Start a project',
  ctaSecondary: 'See selected work',
  ctaPrimaryHref: '#contact-form',
  ctaSecondaryHref: '#portfolio',
  proofTitle: 'Public proof',
  proofItems: [
    { value: '3 live sites', label: 'publicly viewable today' },
    { value: '2 PyPI packages', label: 'installable now' },
    { value: 'CI + CodeQL', label: 'visible in public repos' },
    { value: 'EN / ES', label: 'client-ready delivery' },
  ],
  panelTitle: 'How I work',
  panelIntro: 'Best fit when a team needs a brittle workflow stabilized quickly, with a clear scope and no appetite for a long custom-software saga.',
  panelItems: [
    {
      label: 'Typical scope',
      value: 'Reporting automation, ETL reliability, scraping workflows, and internal data tools.',
    },
    {
      label: 'Delivery style',
      value: 'Scoped implementation, direct stakeholder communication, and documentation built for handoff.',
    },
    {
      label: 'Timezone fit',
      value: 'Santiago-based, working in English and Spanish across LATAM and U.S.-friendly hours.',
    },
  ],
  noteTitle: 'Standard handoff',
  noteBody: 'CI, tests, and operator notes are part of the default delivery whenever the workflow needs to outlive the build.',
};

const es = {
  availability: 'Disponible para proyectos de automatización acotados',
  meta: 'Tooltician · Carlos Ortega González',
  title: 'Automatización Python para flujos que deben seguir funcionando después del traspaso.',
  summary: 'Construyo sistemas para reportes, ETL y herramientas internas que reemplazan trabajo manual frágil por flujos que un equipo puede operar, auditar y confiar.',
  support: 'Trabajo desde Santiago de Chile, en español e inglés, con equipos de LATAM y zonas horarias compatibles con EE.UU.',
  highlights: [
    'Automatización de reportes, pipelines ETL y flujos de captura de datos pensados para operación repetible.',
    'APIs internas y tooling entregados con CI, pruebas y documentación de traspaso.',
    'Comunicación directa con stakeholders técnicos y no técnicos en español o inglés.',
  ],
  ctaPrimary: 'Iniciar proyecto',
  ctaSecondary: 'Ver trabajo seleccionado',
  ctaPrimaryHref: '#contact-form',
  ctaSecondaryHref: '#portfolio',
  proofTitle: 'Prueba pública',
  proofItems: [
    { value: '3 sitios activos', label: 'visibles hoy' },
    { value: '2 paquetes PyPI', label: 'instalables ahora' },
    { value: 'CI + CodeQL', label: 'visible en repos públicos' },
    { value: 'ES / EN', label: 'entrega lista para clientes' },
  ],
  panelTitle: 'Cómo trabajo',
  panelIntro: 'Encaja mejor cuando un equipo necesita estabilizar un flujo frágil con alcance claro y sin convertirlo en una saga larga de software a medida.',
  panelItems: [
    {
      label: 'Alcance típico',
      value: 'Automatización de reportes, confiabilidad ETL, scraping y herramientas internas de datos.',
    },
    {
      label: 'Forma de entrega',
      value: 'Implementación acotada, comunicación directa con stakeholders y documentación pensada para traspaso.',
    },
    {
      label: 'Cobertura horaria',
      value: 'Trabajo desde Santiago, en español e inglés, con equipos de LATAM y horarios compatibles con EE.UU.',
    },
  ],
  noteTitle: 'Traspaso estándar',
  noteBody: 'CI, pruebas y notas para operadores forman parte de la entrega base cuando el flujo debe seguir funcionando después del build.',
};

const c = lang === 'en' ? en : es;
---
<header class="hero" id="overview">
  <div class="hero__bg" aria-hidden="true"></div>
  <div class="container">
    <div class="hero__layout">
      <div class="hero__copy">
        <div class="hero__meta reveal">
          <span>{c.meta}</span>
          <span>{c.availability}</span>
        </div>
        <h1 class="hero__title reveal reveal-delay-1">{c.title}</h1>
        <p class="hero__summary reveal reveal-delay-2">{c.summary}</p>
        <p class="hero__support reveal reveal-delay-3">{c.support}</p>
        <div class="hero__actions reveal reveal-delay-3">
          <a class="btn btn-primary" href={c.ctaPrimaryHref}>{c.ctaPrimary}</a>
          <a class="btn btn-ghost" href={c.ctaSecondaryHref}>{c.ctaSecondary}</a>
        </div>
        <div class="hero__proofbar reveal reveal-delay-4" aria-label={c.proofTitle}>
          {c.proofItems.map(item => (
            <div class="hero__proofitem">
              <span class="hero__proofvalue">{item.value}</span>
              <span class="hero__prooflabel">{item.label}</span>
            </div>
          ))}
        </div>
        <ul class="hero__highlights reveal reveal-delay-4">
          {c.highlights.map(h => (
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              {h}
            </li>
          ))}
        </ul>
      </div>

      <div class="hero__aside reveal reveal-delay-2">
        <section class="hero__brief" aria-label={c.panelTitle}>
          <p class="hero__brief-kicker">{c.panelTitle}</p>
          <p class="hero__brief-intro">{c.panelIntro}</p>
          <dl class="hero__brief-list">
            {c.panelItems.map(item => (
              <div class="hero__brief-row">
                <dt>{item.label}</dt>
                <dd>{item.value}</dd>
              </div>
            ))}
          </dl>
        </section>
        <aside class="hero__note" aria-label={c.noteTitle}>
          <p class="hero__note-title">{c.noteTitle}</p>
          <p>{c.noteBody}</p>
        </aside>
      </div>
    </div>
  </div>
</header>

<style>
.hero {
  display: flex;
  align-items: center;
  padding-top: calc(var(--nav-height) + 4rem);
  padding-bottom: 5rem;
  position: relative;
  overflow: hidden;
  border-bottom: 1px solid var(--clr-border);
}
.hero__bg {
  position: absolute;
  inset: 0;
  pointer-events: none;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.36), rgba(255, 255, 255, 0) 58%);
}

.hero__layout {
  display: grid;
  grid-template-columns: minmax(0, 1.15fr) minmax(300px, 0.85fr);
  gap: 3.5rem;
  align-items: start;
}
.hero__copy {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
}
.hero__meta {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem 1rem;
  margin-bottom: 1.15rem;
  font-family: var(--ff-label);
  font-size: 0.72rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--clr-text-muted);
}
.hero__title {
  font-family: var(--ff-heading);
  font-size: clamp(3rem, 5.8vw, 5.25rem);
  font-weight: 700;
  line-height: 0.95;
  letter-spacing: -0.045em;
  margin-bottom: 1.2rem;
  max-width: 11.5ch;
  text-wrap: balance;
}
.hero__summary {
  font-size: clamp(1.03rem, 1.8vw, 1.18rem);
  color: var(--clr-text);
  line-height: 1.72;
  margin-bottom: 1.1rem;
  max-width: 58ch;
}
.hero__support {
  font-size: 0.96rem;
  color: var(--clr-text-muted);
  line-height: 1.65;
  margin-bottom: 1.7rem;
  max-width: 58ch;
}
.hero__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-bottom: 1.8rem;
}
.hero__actions .btn {
  padding: 0.95rem 1.2rem;
}
.hero__actions .btn-ghost {
  background: rgba(255, 255, 255, 0.56);
}
.hero__proofbar {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-bottom: 1.7rem;
}
.hero__proofitem {
  min-width: 150px;
  flex: 1 1 170px;
  padding: 0.95rem 1rem;
  border: 1px solid var(--clr-border);
  border-top: 3px solid var(--clr-accent);
  border-radius: 14px;
  background: rgba(255, 255, 255, 0.68);
}
.hero__proofvalue {
  display: block;
  font-family: var(--ff-label);
  font-size: 0.82rem;
  font-weight: 700;
  color: var(--clr-text);
  letter-spacing: 0.08em;
  margin-bottom: 0.28rem;
  text-transform: uppercase;
}
.hero__prooflabel {
  display: block;
  font-size: 0.8rem;
  line-height: 1.45;
  color: var(--clr-text-muted);
}
.hero__highlights {
  display: flex;
  flex-direction: column;
  gap: 0.85rem;
}
.hero__highlights li {
  display: flex;
  align-items: flex-start;
  gap: 0.65rem;
  font-size: 0.94rem;
  color: var(--clr-text-muted);
}
.hero__highlights li svg {
  flex-shrink: 0;
  margin-top: 2px;
  color: var(--clr-accent);
}

.hero__aside {
  position: relative;
  z-index: 1;
  display: grid;
  gap: 1rem;
  align-content: start;
}
.hero__brief {
  width: min(380px, 100%);
  margin-left: auto;
  padding: 1.45rem;
  border: 1px solid var(--clr-border);
  border-radius: 18px;
  background: rgba(255, 255, 255, 0.74);
}
.hero__brief-kicker {
  font-family: var(--ff-label);
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--clr-accent);
  margin-bottom: 0.75rem;
}
.hero__brief-intro {
  font-size: 0.95rem;
  line-height: 1.65;
  color: var(--clr-text);
  margin-bottom: 1rem;
}
.hero__brief-list {
  display: grid;
  gap: 0.85rem;
}
.hero__brief-row {
  display: grid;
  gap: 0.28rem;
  padding-top: 0.85rem;
  border-top: 1px solid rgba(31, 26, 21, 0.09);
}
.hero__brief-row dt {
  font-family: var(--ff-label);
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--clr-text-faint);
}
.hero__brief-row dd {
  font-size: 0.9rem;
  line-height: 1.6;
  color: var(--clr-text);
}
.hero__note {
  width: min(380px, 100%);
  margin-left: auto;
  padding: 1rem 1.15rem;
  border: 1px solid rgba(41, 87, 78, 0.18);
  border-left-width: 4px;
  border-radius: 14px;
  background: rgba(255, 255, 255, 0.66);
}
.hero__note-title {
  font-family: var(--ff-label);
  font-size: 0.72rem;
  font-weight: 600;
  color: var(--clr-accent-2);
  letter-spacing: 0.1em;
  margin-bottom: 0.45rem;
  text-transform: uppercase;
}
.hero__note p:last-child {
  font-size: 0.88rem;
  line-height: 1.6;
  color: var(--clr-text-muted);
}

@media (max-width: 1024px) {
  .hero {
    padding-top: calc(var(--nav-height) + 2rem);
  }
  .hero__layout {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }
  .hero__copy {
    order: 1;
  }
  .hero__aside {
    order: 2;
  }
  .hero__brief,
  .hero__note {
    width: 100%;
    margin-left: 0;
  }
  .hero__proofbar {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .hero {
    padding-top: calc(var(--nav-height) + 1.5rem);
    padding-bottom: 3rem;
  }
  .hero__title {
    max-width: none;
  }
  .hero__summary,
  .hero__support {
    max-width: none;
  }
  .hero__layout {
    gap: 2rem;
  }
}

@media (max-width: 640px) {
  .hero__meta {
    gap: 0.35rem 0.75rem;
    font-size: 0.8rem;
  }
  .hero__actions {
    flex-direction: column;
    align-items: stretch;
  }
  .hero__actions .btn {
    justify-content: center;
  }
  .hero__highlights li {
    font-size: 0.88rem;
  }
  .hero__proofbar {
    grid-template-columns: 1fr;
  }
  .hero__brief,
  .hero__note {
    padding-left: 1rem;
    padding-right: 1rem;
  }
}
</style>
