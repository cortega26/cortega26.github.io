---
import ServicePage from '../../../../components/ServicePage.astro';
import { services } from '../../../../data/services';
import { alternatesFor } from '../../../../data/routes';
import '../../../../styles/global.css';

const def = services['python-automation'];
const canonical = 'https://tooltician.com/en/services/python-automation/';
const crossLocaleHref = '/es/servicios/automatizacion-python/';
const alternates = alternatesFor('service:' + def.serviceKey);
---
<ServicePage
  lang="en"
  content={def.en}
  slug={def.slugEn}
  serviceKey={def.serviceKey}
  canonical={canonical}
  alternates={alternates}
  crossLocaleHref={crossLocaleHref}
  areaServed={def.areaServed}
/>
