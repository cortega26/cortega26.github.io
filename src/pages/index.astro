---
// Redirect root to English version. Language switcher lives in the navbar.
// This eliminates the conversion-killing splash page while keeping /es/ fully accessible.
export async function GET() {
  return new Response(null, {
    status: 301,
    headers: { Location: '/en/' },
  });
}
---
